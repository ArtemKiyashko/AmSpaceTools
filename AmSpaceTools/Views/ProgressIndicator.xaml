<UserControl x:Class="AmSpaceTools.Views.ProgressIndicator"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AmSpaceTools.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Row="0"
                    Grid.Column="0" 
                    Orientation="Horizontal"
                    DataContext="{Binding Progress}">
            <TextBlock >Progress:</TextBlock>
            <TextBlock Text = "{Binding ProgressPercent, Mode=OneWay}">
            </TextBlock>
        </StackPanel>
        <StackPanel Grid.Row="0"
                    Grid.Column="1"
                    Orientation="Horizontal"
                    DataContext="{Binding Progress}">
            <TextBlock>Items processed:</TextBlock>
            <TextBlock Text="{Binding ProgressTasksDone }">
            </TextBlock>
            <TextBlock> / </TextBlock>
            <TextBlock Text="{Binding ProgressTasksTotal}">
            </TextBlock>
        </StackPanel>
        <StackPanel Grid.Row="0"
                    Grid.Column="2"
                    DataContext="{Binding Progress}">
            <TextBlock >Status:</TextBlock>
            <TextBlock Text="{Binding ProgressStatus}">
            </TextBlock>
        </StackPanel>
        <ProgressBar Grid.Column="0"
                     Grid.Row="1"
                     Grid.ColumnSpan="3"
                     Value="{Binding Progress.ProgressPercent, Mode=OneWay}"
                     Margin="10"/>
        <Button Grid.Row="2" Grid.Column="1" Command="{Binding CancelCommand, Mode=OneWay}" Content="Cancel"/>
    </Grid>
</UserControl>
